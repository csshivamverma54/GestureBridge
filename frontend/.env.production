# Production — React bundle is served by Flask on the same origin.
# VITE_API_URL must be empty so every /video, /predict, etc. URL resolves
# to the current deployment origin instead of a hardcoded address.
# Override only if React and Flask are on DIFFERENT origins (e.g. CDN + Render).
VITE_API_URL=
