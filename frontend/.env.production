# Production: React is served by Flask on the same origin.
# VITE_API_URL must be empty so all API calls use relative paths.
# Do NOT set this to a hardcoded URL unless React and Flask are on different origins.
VITE_API_URL=
