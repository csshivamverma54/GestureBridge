# Development — Vite dev server on :3000, Flask on :5000.
# Leave VITE_API_URL empty: all API/video paths are proxied by Vite (see vite.config.js).
# DO NOT set this to http://localhost:5000 — that would break video URL building.
VITE_API_URL=
